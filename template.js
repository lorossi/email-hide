window.onload=function ${function_name}(){let ${container_variable}="${encoded_container}";let ${email_variable}="${encoded_email}";for (let ${index_variable}=0;${index_variable}<${iterations};${index_variable}++){${container_variable}=atob(${container_variable});${email_variable}=atob(${email_variable});}document.querySelector(${container_variable}).innerHTML=${email_variable};}

window.onload=function ${function_name}(){\n\tlet ${container_variable}="${encoded_container}";\n\tlet ${email_variable}="${encoded_email}";\n\tfor (let ${index_variable}=0;${index_variable}<${iterations};${index_variable}++){\n\t\t${container_variable}=atob(${container_variable});\n\t\t${email_variable}=atob(${email_variable});\n\t}\n\tdocument.querySelector(${container_variable}).innerHTML=${email_variable};}
